# Local secrets (this file is gitignored — copy values as needed)
# Optional: overrides appsettings REDIS_CONNECTION_STRING / JWT / SendGrid
REDIS_CONNECTION_STRING=localhost:6380,abortConnect=false
JWT_KEY=LOCAL_DEV_ONLY_replace_via_JWT_KEY_env_in_shared_environments_32chars+
SENDGRID_API_KEY=
